const numbers = [1, 2, 3, 4, 5];

const doubledNumbers = numbers.map(num => num * 2)

let result = []

for (let nuber = 0; number < numbers.length; nuber++) {
    result.push(numbers[number] * 2)
    
}

console.log(result);

